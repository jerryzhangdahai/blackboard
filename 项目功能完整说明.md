# 协同白板项目 - 功能完整说明

## 项目概述

一个基于 **React + tldraw + WebSocket + Node.js** 的在线团队协作白板系统，使用原生 tldraw 作为核心渲染引擎，支持多人实时绘图、URL 分享、丰富的图形绘制工具、性能优化和智能内存管理等功能。

### 技术栈

- **前端**：React 19 + TypeScript + Vite + tldraw 4.3.0
- **后端**：Node.js + Express + WebSocket (ws)
- **架构**：基于 tldraw 的原生协同架构、TLStore 状态管理、RecordsDiff 增量同步

---

## 一、基础功能模块

### 1. 图形绘制工具（基于 tldraw 原生能力）

#### 1.1 自由线条（画笔工具）
- **功能**：使用 tldraw 的 `draw` 工具支持自由手绘线条
- **特性**：
  - 实时绘制预览（tldraw 原生支持）
  - 支持颜色、线宽、线型（实线/虚线）自定义
  - 平滑的绘制体验（tldraw 内置平滑算法）
  - 自动路径优化和分段处理

#### 1.2 基本图形绘制
- **矩形工具**：使用 tldraw 的 `geo` shape（rectangle）
- **圆形工具**：使用 tldraw 的 `geo` shape（ellipse）
- **三角形工具**：使用 tldraw 的 `geo` shape（triangle）
- **菱形工具**：使用 tldraw 的 `geo` shape（diamond）
- **箭头工具**：使用 tldraw 的 `arrow` shape
- **文本工具**：使用 tldraw 的 `text` shape

**所有图形支持**（tldraw 原生特性）：
- 颜色自定义（通过 `props.color`）
- 线条粗细调整（通过 `props.size`）
- 线条样式选择（实线/虚线，通过 `props.dash`）
- 填充样式（none/solid/semi，通过 `props.fill`）
- 实时绘制预览和交互式编辑

### 2. 图形编辑功能（tldraw 原生能力）

#### 2.1 选择与拖拽
- **选择工具**：使用 tldraw 默认选择工具
  - 点击图形进行选中（tldraw 自动处理命中测试）
  - 选中图形显示高亮边框和调整手柄
  - 支持多选（框选或 Ctrl+点击）
  - 支持显示选中图形信息（类型、属性）
- **拖拽移动**：选中图形后可以拖拽到新位置
  - 实时预览拖拽位置（tldraw 原生支持）
  - 支持所有图形类型的拖拽
  - 支持旋转和缩放（通过调整手柄）

#### 2.2 属性编辑
- **颜色修改**：
  - 通过 tldraw 工具栏修改选中图形的颜色
  - 支持所有图形类型的颜色自定义
  - 颜色修改实时同步到所有客户端
- **线宽调整**：
  - 通过 tldraw 工具栏调整线条粗细（s/m/l/xl）
  - 支持所有图形类型的线宽修改
- **线型切换**：
  - 实线/虚线切换（通过 `props.dash`）
  - 填充样式切换（none/solid/semi，通过 `props.fill`）

#### 2.3 删除功能
- **选择删除**：选中图形后按 Delete 键或点击删除按钮
- **橡皮擦工具**：使用 tldraw 的橡皮擦工具
  - 点击删除（整图删除）
  - 拖动删除（局部擦除，tldraw 原生支持）

### 3. 撤销与重做（tldraw 原生能力）

#### 3.1 撤销（Undo）
- **功能**：使用 tldraw 内置的撤销功能
- **快捷键**：`Ctrl + Z`（Windows/Linux）或 `Cmd + Z`（Mac）
- **支持范围**：所有编辑操作（新增、修改、删除、移动等）
- **实时同步**：撤销操作通过 `RecordsDiff` 同步到所有连接的客户端

#### 3.2 重做（Redo）
- **功能**：使用 tldraw 内置的重做功能
- **快捷键**：`Ctrl + Y` 或 `Ctrl + Shift + Z`（Windows/Linux）或 `Cmd + Shift + Z`（Mac）
- **支持范围**：撤销的操作可以重做
- **实时同步**：重做操作通过 `RecordsDiff` 同步到所有连接的客户端

### 4. 快捷键支持（tldraw 原生快捷键）

- **工具切换**：使用 tldraw 默认快捷键
  - `V`：切换到选择工具
  - `D`：切换到画笔工具
  - `R`：切换到矩形工具
  - `O`：切换到圆形工具
  - `A`：切换到箭头工具
  - `T`：切换到文本工具
  - `E`：切换到橡皮擦工具
- **编辑操作**：
  - `Ctrl/Cmd + Z`：撤销
  - `Ctrl/Cmd + Y` 或 `Ctrl/Cmd + Shift + Z`：重做
  - `Delete` 或 `Backspace`：删除选中图形
  - `Ctrl/Cmd + A`：全选
  - `Ctrl/Cmd + C`：复制
  - `Ctrl/Cmd + V`：粘贴

---

## 二、协同功能模块

### 1. 多人实时协作（基于 tldraw TLStore 协同）

#### 1.1 WebSocket 实时同步
- **连接方式**：基于 WebSocket 的双向通信
- **消息类型**：
  - `init`：新客户端连接时发送完整的 `TLStoreSnapshot`，用于初始化画布
  - `snapshot`：全量快照同步（用于新客户端初始化或降级场景）
  - `diff`：增量变更同步（`RecordsDiff`），用于实时协同编辑
- **数据格式**：
  - 使用 tldraw 的 `TLStoreSnapshot` 存储完整状态
  - 使用 tldraw 的 `RecordsDiff` 进行增量同步
  - 每个消息包含 `clientId` 用于过滤自身消息

#### 1.2 实时状态同步
- **绘图同步**：通过 `store.listen` 监听用户操作，发送 `RecordsDiff` 到服务端并广播
- **属性修改同步**：图形属性（颜色、线宽、线型）修改通过 `RecordsDiff` 实时同步
- **删除同步**：图形删除操作通过 `RecordsDiff` 实时同步
- **拖拽同步**：图形移动操作通过 `RecordsDiff` 实时同步
- **撤销/重做同步**：撤销和重做操作通过 `RecordsDiff` 实时同步
- **性能优化**：
  - 批量合并多个 `diff`（使用 `squashRecordDiffs`）
  - 使用 `requestAnimationFrame` 批量应用远程变更
  - 大 `diff`（超过 1000 条记录）自动降级为 `snapshot` 同步
  - 画笔绘制时延迟发送，避免长笔画被拆成多段

#### 1.3 URL 分享功能
- **自动生成**：访问页面时自动生成唯一的白板 ID
- **URL 格式**：`http://localhost:5173/?boardId=xxxxxx`
- **分享方式**：复制完整链接分享给其他用户
- **多端支持**：支持不同浏览器窗口、不同设备访问同一白板

---

## 三、高级功能模块

### 1. 橡皮擦功能（tldraw 原生能力）

#### 1.1 点击删除模式
- **功能**：使用 tldraw 的橡皮擦工具点击图形直接删除整个图形
- **适用对象**：所有图形类型（矩形、圆形、三角形、菱形、箭头、文本、线条）
- **交互**：选择橡皮擦工具，然后在图形上点击

#### 1.2 拖动擦除模式（局部擦除）
- **线条擦除**：
  - 橡皮擦路径经过的线段会被删除
  - 剩余部分自动拆分成多条新线段（tldraw 自动处理）
  - 视觉效果：线条被擦断、切成几段
- **图形轮廓擦除**：
  - 矩形/圆形/三角形/菱形/箭头的轮廓被擦除后
  - 被擦掉的轮廓段会删除
  - 剩余的轮廓段转换成笔画（tldraw 自动处理）
  - 视觉效果：图形轮廓被挖掉一截，变成几段线
- **文本处理**：
  - 文本不支持局部擦除（不把文字拆碎）
  - 只支持点击删除整图

### 2. tldraw 原生架构优势

#### 2.1 无限画布能力
- **缩放和平移**：tldraw 原生支持无限画布、缩放、平移
- **多选和分组**：支持框选、多选、图形分组
- **对齐和分布**：支持图形对齐和分布功能

#### 2.2 性能优化（tldraw 内置）
- **虚拟化渲染**：只渲染可见区域的图形
- **自动优化**：tldraw 内置性能优化，无需手动实现空间索引
- **增量更新**：基于 `RecordsDiff` 的增量同步，只传输变更部分

#### 2.3 扩展性
- **自定义 Shape**：可以通过 tldraw 的自定义 Shape API 扩展新的图形类型
- **自定义 Tool**：可以通过 tldraw 的自定义 Tool API 扩展新的工具
- **插件系统**：tldraw 支持插件系统，可以扩展更多功能

---

## 四、性能优化模块

### 1. tldraw 原生性能优化

#### 1.1 虚拟化渲染
- **原理**：tldraw 自动实现虚拟化渲染，只渲染可见区域的图形
- **性能提升**：即使有数万个图形，也能保持流畅的交互体验
- **自动优化**：无需手动实现空间索引或增量绘制

#### 1.2 命中测试优化
- **内置优化**：tldraw 内置高效的命中测试算法
- **自动缓存**：自动缓存命中测试结果，避免重复计算
- **性能表现**：支持大量重叠图形的快速选择

### 2. 协同同步性能优化

#### 2.1 增量同步机制
- **原理**：使用 tldraw 的 `RecordsDiff` 进行增量同步，只传输变更部分
- **工作流程**：
  1. 通过 `store.listen` 监听用户操作，生成 `RecordsDiff`
  2. 批量合并多个 `diff`（使用 `squashRecordDiffs`）
  3. 通过 WebSocket 发送到服务端并广播
  4. 客户端接收后使用 `store.applyDiff` 应用变更
- **性能提升**：只传输变更数据，大幅减少网络传输量

#### 2.2 批量处理优化
- **批量合并**：使用 `squashRecordDiffs` 合并多个 `diff`
- **批量应用**：使用 `requestAnimationFrame` 批量应用远程变更
- **降级策略**：大 `diff`（超过 1000 条记录）自动降级为 `snapshot` 同步

#### 2.3 画笔绘制优化
- **延迟发送**：画笔绘制时延迟发送 `diff`，避免长笔画被拆成多段
- **智能合并**：等待绘制完成后一次性发送完整的笔画
- **性能表现**：支持流畅的长笔画绘制，不会产生卡顿

### 3. 内存管理优化

#### 3.1 快照压缩
- **原理**：服务端存储完整的 `TLStoreSnapshot`，定期压缩
- **压缩策略**：超过阈值时自动压缩，只保留必要的状态
- **内存节省**：大幅减少服务端内存占用

#### 3.2 增量更新
- **原理**：客户端只存储和应用增量变更，不存储完整快照
- **内存节省**：客户端内存占用大幅减少
- **性能提升**：应用变更的速度更快

---

## 五、UI/UX 优化模块

### 1. tldraw 原生 UI 优势

#### 1.1 现代化界面
- **工具栏**：tldraw 提供现代化的工具栏，支持工具切换、属性编辑
- **菜单系统**：右键菜单、上下文菜单等丰富的交互方式
- **快捷键提示**：内置快捷键提示，提升操作效率

#### 1.2 响应式设计
- **自适应布局**：自动适配不同屏幕尺寸
- **触摸支持**：支持触摸设备，可以用于平板和手机
- **多设备兼容**：支持桌面、平板、手机等多种设备

### 2. 自定义 UI 扩展

#### 2.1 选中图形提示栏（可选）
- **功能**：可以基于 tldraw 的 overlay 系统实现自定义提示栏
- **位置**：悬浮在工具栏下方，不占用文档流空间
- **功能**：显示选中图形的类型、属性，提供快速删除按钮

#### 2.2 分享链接 UI
- **功能**：在白板页面顶部显示分享链接
- **交互**：点击复制链接，方便分享给其他用户

---

## 六、服务端功能模块

### 1. 内存管理（适配 tldraw 数据结构）

#### 1.1 活动时间追踪
- **创建时间**：记录每个白板的创建时间
- **最后活动时间**：记录每个白板的最后活动时间
- **客户端断开时间**：记录最后一个客户端断开的时间
- **更新机制**：每次 WebSocket 连接、消息、断开都更新活动时间

#### 1.2 TTL 过期清理
- **过期策略**：白板在指定时间内（默认 24 小时）无活动且无客户端连接时自动删除
- **保护机制**：有客户端连接的白板不会被清理
- **可配置**：TTL 时间可通过环境变量配置（`TTL=毫秒数`）
- **动态清理间隔**：如果 TTL 较短（< 5 分钟），清理间隔自动调整为 `TTL / 2`（最小 1 秒）

#### 1.3 LRU 数量限制
- **数量限制**：限制最大白板数量（默认 100 个）
- **LRU 策略**：当达到上限时，优先删除最久未使用的白板
- **实现方式**：按 `lastActiveAt` 排序，删除最旧的无客户端白板
- **保护机制**：优先删除无客户端的白板，保护正在使用的白板
- **渐进式清理**：每次最多清理 10 个白板，使用 `setImmediate()` 分批执行

#### 1.4 渐进式清理策略
- **分批清理**：每次清理最多处理 N 个白板（默认 10 个）
- **优先级排序**：按优先级排序，优先删除最旧、无客户端、snapshot 小的白板
- **异步执行**：使用 `setImmediate()` 延迟执行下一批，避免性能冲击

#### 1.5 智能清理频率调整
- **动态调整**：根据内存使用率动态调整清理间隔
  - 内存使用率 > 90%：1 分钟清理一次（紧急）
  - 内存使用率 > 75%：2.5 分钟清理一次
  - 内存使用率 > 50%：5 分钟清理一次（默认）
  - 内存使用率 ≤ 50%：30 分钟清理一次
- **内存监控**：使用 `process.memoryUsage()` 实时监控内存使用情况

#### 1.6 白板数据存储
- **存储格式**：使用 tldraw 的 `TLStoreSnapshot` 存储完整状态
- **大小计算**：通过 `snapshotBytes` 字段记录快照大小（字节数）
- **内存估算**：基于 `snapshotBytes` 估算每个白板的内存占用

### 2. 监控接口

#### 2.1 健康检查接口
- **路径**：`GET /health`
- **功能**：返回服务器健康状态
- **返回内容**：
  - 内存使用情况（堆内存使用、总量、使用率）
  - 内存状态（ok/warning）
  - 白板统计（总数、活跃数）

#### 2.2 统计接口
- **路径**：`GET /api/stats`
- **功能**：返回详细的统计信息
- **返回内容**：
  - 白板统计（总数、活跃数、非活跃数）
  - 快照统计（总快照大小、平均每个白板）
  - 内存统计（进程内存、估算内存、使用率）
  - 配置信息（最大白板数、TTL、清理间隔等）
  - 清理日志统计

#### 2.3 清理日志接口
- **路径**：`GET /api/cleanup-logs?limit=N`
- **功能**：返回清理日志
- **参数**：`limit`（可选，默认 100）
- **返回内容**：
  - 清理日志列表（时间戳、白板 ID、清理原因、操作数、释放内存）
  - 总日志数量

---

## 七、技术实现细节

### 1. tldraw 协同实现

#### 1.1 客户端实现
- **编辑器初始化**：使用 `<Tldraw>` 组件和 `onMount` 回调初始化编辑器
- **状态监听**：通过 `store.listen` 监听用户操作，生成 `RecordsDiff`
- **消息发送**：
  - 普通变更：发送 `diff` 消息（16ms 节流）
  - 画笔绘制：延迟发送，等待绘制完成后发送完整笔画
  - 兜底机制：每 1000ms 发送一次 `snapshot`，确保新客户端能正确初始化
- **消息接收**：
  - 过滤自身消息：通过 `clientId` 过滤自己发送的消息
  - 批量应用：使用 `pendingDiffsRef` 批量合并多个 `diff`
  - 性能优化：大 `diff`（> 1000 条记录）自动降级为 `snapshot`

#### 1.2 服务端实现
- **数据存储**：使用 `TLStoreSnapshot` 存储每个白板的状态
- **消息处理**：
  - `init`：发送完整 `snapshot` 给新客户端
  - `snapshot`：更新白板状态并广播给其他客户端
  - `diff`：直接转发给其他客户端（不解析内容）
- **内存管理**：基于 `snapshotBytes` 估算内存占用，实现 TTL 和 LRU 清理

### 2. 性能优化实现

#### 2.1 画笔绘制优化
- **问题**：tldraw 会将长笔画拆分成多个 `draw` shape
- **解决方案**：延迟发送 `draw` 类型的 `diff`，等待绘制完成后一次性发送
- **实现**：
  - 检测 `diff` 中是否包含 `draw` 类型的 shape
  - 如果是 `draw` 类型，暂存 `diff` 并设置延迟发送
  - 监听 `pointerup` 事件，等待绘制完成后发送完整笔画

#### 2.2 批量处理优化
- **批量合并**：使用 `squashRecordDiffs` 合并多个 `diff`
- **批量应用**：使用 `requestAnimationFrame` 批量应用远程变更
- **性能提升**：减少 DOM 操作次数，提升渲染性能

---

## 八、架构特性

### 1. 模块化设计

#### 1.1 前端模块化
- **组件分离**：`WhiteboardPage`（页面容器）和 `Whiteboard`（核心逻辑）分离
- **tldraw 集成**：使用 `<Tldraw>` 组件作为核心渲染引擎
- **协同逻辑**：WebSocket 通信和状态同步逻辑封装在 `Whiteboard` 组件中

#### 1.2 服务端模块化
- **WebSocket 服务**：处理实时协同通信
- **HTTP API**：提供健康检查、统计、清理日志等接口
- **内存管理**：独立的清理和监控模块

### 2. 可扩展性

#### 2.1 tldraw 扩展能力
- **自定义 Shape**：可以通过 tldraw 的自定义 Shape API 扩展新的图形类型
- **自定义 Tool**：可以通过 tldraw 的自定义 Tool API 扩展新的工具
- **插件系统**：tldraw 支持插件系统，可以扩展更多功能

#### 2.2 配置化
- **环境变量配置**：服务端参数可通过环境变量配置
  - `MAX_BOARDS`：最大白板数量（默认 100）
  - `TTL`：过期时间（毫秒，默认 24 小时）
  - `CLEANUP_INTERVAL`：基础清理间隔（毫秒，默认 5 分钟）
  - `MAX_CLEANUP_PER_BATCH`：每次清理最多 N 个（默认 10）
  - `WARN_THRESHOLD`：内存警告阈值（默认 0.8）

---

## 九、项目结构

```
协同白板项目/
├── client/                          # 前端应用
│   ├── src/
│   │   ├── components/
│   │   │   └── Whiteboard.tsx      # 核心白板组件（基于 tldraw）
│   │   ├── WhiteboardPage.tsx      # 页面容器
│   │   ├── App.tsx                 # 应用入口
│   │   ├── App.css                 # 样式文件
│   │   └── main.tsx                # 入口文件
│   ├── package.json                # 依赖配置（包含 tldraw）
│   └── vite.config.ts              # Vite 配置
├── server/
│   └── index.js                    # 服务端入口（WebSocket + HTTP）
├── test-incremental-drawing.js     # tldraw 协同性能测试脚本
├── test-hittest-performance.js     # tldraw hitTest 性能测试脚本
├── test-memory-pressure.js         # 服务端内存管理测试脚本
├── start-dev.ps1                   # 开发环境启动脚本（PowerShell）
├── start-dev.bat                   # 开发环境启动脚本（批处理）
├── README.md                       # 项目说明
└── 项目功能完整说明.md             # 本文档
```

---

## 十、功能总结

### 基础功能 ✅
- [x] 自由线条绘制（tldraw 画笔工具）
- [x] 基本图形绘制（矩形、圆形、三角形、菱形、箭头、文本）
- [x] 图形属性编辑（颜色、线宽、线型、填充）
- [x] 图形选择与拖拽（tldraw 原生支持）
- [x] 图形删除（Delete 键或工具栏）
- [x] 撤销与重做（tldraw 原生支持）
- [x] 快捷键支持（tldraw 原生快捷键）

### 协同功能 ✅
- [x] 多人实时协作（基于 tldraw TLStore）
- [x] WebSocket 实时同步（snapshot + diff）
- [x] URL 分享功能
- [x] 增量同步优化（RecordsDiff）

### 高级功能 ✅
- [x] 橡皮擦工具（点击删除 + 拖动擦除，tldraw 原生支持）
- [x] 无限画布（缩放、平移，tldraw 原生支持）
- [x] 多选和分组（tldraw 原生支持）
- [x] 对齐和分布（tldraw 原生支持）

### 性能优化 ✅
- [x] 虚拟化渲染（tldraw 内置）
- [x] 增量同步优化（RecordsDiff + 批量处理）
- [x] 画笔绘制优化（延迟发送，避免分段）
- [x] 大 diff 降级策略（自动降级为 snapshot）

### UI/UX 优化 ✅
- [x] 现代化界面（tldraw 原生 UI）
- [x] 响应式设计（支持多设备）
- [x] 触摸支持（支持平板和手机）
- [x] 分享链接 UI

### 服务端功能 ✅
- [x] 内存管理（TTL + LRU + 活动追踪）
- [x] 智能清理频率调整
- [x] 白板数据存储（TLStoreSnapshot）
- [x] 监控接口（健康检查、统计、清理日志）
- [x] 清理 API（DELETE /api/boards）

### 测试脚本 ✅
- [x] tldraw 协同性能测试（test-incremental-drawing.js）
- [x] tldraw hitTest 性能测试（test-hittest-performance.js）
- [x] 服务端内存管理测试（test-memory-pressure.js）

---

## 十一、技术亮点

1. **基于成熟框架**：
   - 使用原生 tldraw 作为核心渲染引擎，减少自研代码
   - 享受 tldraw 的持续更新和性能优化
   - 支持无限画布、缩放、平移等专业功能

2. **高性能协同**：
   - 基于 tldraw TLStore 的增量同步（RecordsDiff）
   - 批量合并和处理，减少网络传输
   - 支持大规模图形（4000+）的流畅协同

3. **内存管理**：
   - 多层防护机制（TTL + LRU + 活动追踪）
   - 智能清理频率调整
   - 内存监控与预警
   - 基于 snapshot 大小的内存估算

4. **用户体验**：
   - 实时协同绘图（毫秒级延迟）
   - 流畅的交互体验（tldraw 原生优化）
   - 丰富的快捷键支持（tldraw 原生快捷键）
   - 支持多设备（桌面、平板、手机）

5. **代码质量**：
   - TypeScript 类型安全
   - 模块化设计
   - 清晰的代码结构
   - 完善的测试脚本

---

**文档创建时间**：2026-01-16  
**文档更新时间**：2026-01-16  
**项目版本**：v2.0.0（基于原生 tldraw）  
**技术栈**：React 19 + TypeScript + Vite + tldraw 4.3.0 + Node.js + WebSocket

## 十二、使用原生 tldraw 替代 Canvas 的整体方案

### 1. 目标与总体思路

- **目标**：将当前前端中所有基于 `<canvas>` + 自研 `DrawOp` / 命中测试 / 增量绘制 / 空间索引的渲染逻辑，逐步迁移为基于原生 tldraw 的实现，同时保留本项目已经实现的“多白板 + 协同 + 内存管理”等服务端能力。
- **核心思路**：
  - 前端 UI 层：用 tldraw 提供的 `<Tldraw>` 组件和 editor API 替代手写 Canvas 渲染。
  - 图形模型层：用 tldraw 自带的 `TLShape` / `TLSnapshot` / `TLStore` 替代自研的 `DrawOp[]` 模型。
  - 协同层：沿用当前 WebSocket + `boardId` 的房间机制，仅调整消息格式，使之同步 tldraw 的 store 变化。
  - 自定义功能：通过 tldraw 的自定义 shape / 自定义 tool 能力，补齐本项目已有的橡皮擦、特殊图形等高级功能。

### 2. 数据模型映射设计

- **现有模型**：位于 `whiteboard-core` 的 `types.ts` 中，核心是：
  - `StrokeOp`（自由线条，points[]）
  - `RectOp`、`CircleOp`、`TriangleOp`、`DiamondOp`、`ArrowOp`
  - `TextOp`
- **tldraw 模型**：基于 `TLShape` / `TLSnapshot` / `TLStore`：
  - 自由线条 → `TLDrawShape`
  - 矩形 / 圆 / 三角形 / 菱形 → `TLGeoShape` 或自定义 `Geo` 子类型
  - 箭头 → `TLArrowShape`
  - 文本 → `TLTextShape`
- **映射策略**：
  - 在前端新增一层“适配器”，负责在需要时将旧的 `DrawOp[]` 转为 tldraw `TLSnapshot`，保证老数据可迁移。
  - 样式（颜色、线宽、线型）统一映射到 tldraw 的 `props`（如 `stroke`, `strokeWidth`, `dash` 等）。
  - 长期方向：新版本前端直接只使用 tldraw 内部模型，不再生成新的 `DrawOp`，旧模型仅用于兼容历史数据。

### 3. 协同同步方案（WebSocket 适配）

- **现状**：
  - 客户端与服务端通过 WebSocket 同步 `DrawOp` 及 `reset`：
    - `init`：发送当前 `ops` 供新客户端重放画面
    - `op`：单条新增绘图操作
    - `reset`：整板状态重置（撤销/重做/橡皮擦/属性修改等）
- **改造方向**：
  - 前端使用 tldraw 的 `TLStore` 作为唯一真相源（single source of truth）。
  - 通过 `store.listen` 监听本地编辑器的变更，生成可序列化的“补丁”或“快照”。
  - 新的 WebSocket 消息建议格式：
    - `snapshot`：全量快照（对应当前的 `reset` 场景）
    - `patch`：基于 tldraw store 的增量补丁（对应当前的单条 `op`）
  - 服务端仍以 `boards` Map 存储每个白板的状态，只是存储的从 `ops: DrawOp[]` 换成 `snapshot: TLSnapshot` 或“初始快照 + 补丁列表”。
- **兼容策略**：
  - 过渡期可以在服务端同时维护“旧的 DrawOp 列表 + 新的 tldraw 快照”，前端根据版本选择使用；待迁移稳定后再完全切换。

### 4. 自定义图形与工具迁移

- **图形（Shape）**：
  - 将当前 `RectHandler`、`CircleHandler`、`TriangleHandler`、`DiamondHandler`、`ArrowHandler` 等处理器逻辑，迁移为 tldraw 的自定义 Shape Util：
    - 每种图形定义其几何、渲染、包围盒与命中测试逻辑。
    - 这样可以复用已有的几何经验，同时享受 tldraw 的渲染与编辑能力。
  - 不再需要自己维护 `hitTest`、`spatialIndex`，改由 tldraw 内建的命中测试与优化负责。
- **工具（Tool）**：
  - 将当前 `Tool`（`pen`, `rect`, `circle`, `triangle`, `diamond`, `arrow`, `text`, `select`, `eraser`）映射为 tldraw 内置或自定义工具：
    - 选择工具 → tldraw 默认选择工具。
    - 画笔 / 形状工具 → tldraw 的 draw / geo 工具或自定义工具。
    - 文本工具 → tldraw 文本工具。
    - 橡皮擦 → 自定义 Eraser Tool：支持“点击整图删除 + 按住拖动局部擦除”两种模式。
- **橡皮擦高级行为**：
  - 原实现中，橡皮擦会将线条/图形轮廓切断，并把剩余部分拆成多条新的 `StrokeOp`。
  - 在 tldraw 中可以用类似策略：将被擦到的 shape 拆分为若干新 shape（多个 draw 线段），并通过单次 patch 同步给其他客户端。

### 5. UI 层替换与保留

- **替换部分**：
  - `Whiteboard.tsx` 中的 `<canvas>` 及其所有鼠标事件处理（绘制、拖拽、擦除、命中测试）将被 `<Tldraw>` 和 tldraw 的工具体系取代。
  - 自己实现的增量绘制、空间索引、hitTest 等逻辑将逐步废弃。
- **保留部分**：
  - `WhiteboardPage.tsx` 中的白板页面结构、分享链接 UI、`boardId` 生成逻辑继续沿用。
  - 顶部工具栏和“已选中图形”悬浮提示栏的 UI 设计可以用 tldraw 的自定义 UI / overlay 重新实现，保持视觉与交互风格不变。

### 6. 分阶段实施步骤（建议）

1. **阶段一：引入 tldraw 与最小可用白板**
   - 在当前项目中安装 tldraw 依赖。
   - 新建一个实验性的 `TldrawWhiteboard` 组件，仅使用本地状态（不接 WebSocket），实现基础绘制（画笔、矩形、文本等），用于验证集成是否顺畅。
2. **阶段二：数据模型与历史数据迁移**
   - 编写 `DrawOp[] <-> TLSnapshot` 的双向转换器。
   - 实现“老白板打开时自动把旧数据转换成 tldraw 状态”的逻辑，确保已有数据不丢失。
3. **阶段三：协同协议改造**
   - 在前端基于 tldraw store 生成 `snapshot` / `patch` 消息，通过 WebSocket 发给服务端。
   - 服务端调整 `init` / `op` / `reset` 处理逻辑，使其存取 tldraw 状态并广播。
   - 在一台机器上用多个浏览器窗口验证多人协作是否稳定。
4. **阶段四：自定义工具与高级功能**
   - 基于 tldraw 的自定义 shape / tool API 重现本项目的橡皮擦、三角形、菱形等高级行为。
   - 将原先基于 Canvas 的细节优化（如局部重绘）迁移到 tldraw 的最佳实践中，删除不再需要的 Canvas 相关代码。
5. **阶段五：性能优化与回归测试**
   - 在大规模图形（上万形状）和多人实时协作场景下做压力测试。
   - 验证撤销/重做、拖拽、属性修改、橡皮擦等关键路径没有明显性能退化。

### 7. 迁移后的收益与注意事项

- **收益**：
  - 大幅减少自研 Canvas 绘图与优化代码，降低维护成本。
  - 引入成熟的无限画布、缩放、平移、多选等能力，整体交互体验更接近专业白板产品。
  - 借助 tldraw 的生态，可以更容易扩展如图片嵌入、连接线绑定、分组等高级功能。
- **注意事项**：
  - tldraw 的渲染基于 DOM/SVG，对“超大规模元素”场景需要实际压测，必要时进行分层、分页或懒加载设计。
  - 协同协议需要统一升级，避免前后端状态不一致；迁移阶段可以设计版本号或兼容模式，平滑过渡。
  - 自定义 shape / tool 的复杂度不低，需要良好封装和文档，以便后续继续扩展。

> 本节为后续“用原生 tldraw 替代当前 Canvas 白板实现”的设计草案，真正实施时可以在此基础上细化任务拆分、预估工作量，并逐步替换现有前端实现。
